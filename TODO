

## 0.8
* tests for traversal
* more tests
* ipv4, ipv6 compatibility checks


## future
* default pwhandler for different environments (e.g. kivy, gtk, windows, tk)
* check if traversal request succeeded
* open traversal in cmdcom
