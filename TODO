
use create_connection with source_address instead of using socket direct for traversal
document behaviour of '' (beeing a wildcard for 0.0.0.0, ::)

## 0.3
* improve tests
* default pwhandler for different environments (e.g. kivy, gtk, windows, tk)
* better code quality
* mass import
* holepunching for services
