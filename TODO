## 0.9
* compile rw_socket for less latency
* traverse_service
* use multiprocessing contexts (spawn) for pwrequests
* tests for traversal
* more tests
* ipv4, ipv6 compatibility checks

## future
* check if traversal request succeeded
* open traversal in cmdcom
